package model.tile;

import contract.tile.IPosition;
import contract.tile.ITileMap;

public class TileBasic extends Tile {
	
	public TileBasic(Sprite sprite, IPosition position, ITileMap map) {
		super(sprite, position, map);
	}

}
